const path = require("path");

module.exports = {
  config: path.resolve("sequelize-config.js"),
  "models-path": path.resolve("src/models"), // your custom models folder
  "seeders-path": path.resolve("src/seeders"), // your custom seeders folder
  "migrations-path": path.resolve("src/migrations"), // your custom migrations folder
};
